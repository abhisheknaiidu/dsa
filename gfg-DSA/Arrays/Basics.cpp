#include <bits/stdc++.h>
using namespace std;

int main() {
    // Reverse an array
    // int arr[] = { 3,5,6,7 };
    // int n = sizeof(arr) / sizeof(arr[0]);
    // reverse(arr, arr+n);
    // for(int i=0; i<n; i++) {
    //     cout << arr[i] << endl;
    // }
    return 0;
}
